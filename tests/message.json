{
    "command": "{{ .Command | json_escape }}",
    "invokedAt": {{ .InvokedAt }},
    "environments": {
        "SHELL": "{{ must_env `SHELL` }}",
        "FOO": "{{ .Env.FOO | json_escape }}"
    }
}
